# Статус — Понеділок, 13 липня 2026

**Ціль дня:** Ознайомитись з клієнтським wireframe, зробити інвентаризацію, почати
порівняння, зафіксувати перші питання.

## Зроблено

### 1. Обхід клієнтського wireframe
Пройшов усі 68 екранів варіанту Гарета (гілка `essentials`).

### 2. Інвентаризація
Зібрав 12 ключових потоків: agentless-вузол, Connection Manager, Credential Vault,
AWS OIDC, розклад скану, помилки скану, історія сканів, baseline, імпорт ServiceNow,
імпорт CSV, звіти, export.

### 3. Порівняння Client vs Bambuk
Через git звірив ID екранів: `essentials` (68) = **строгий суперсет** нашого `dev` (55).
→ **13 нових екранів** у клієнта, **0 викинутих**.
Підтверджує слова Гарета: "повністю включено в мою версію".

**13 нових екранів:**

| # | ID екрана | Що це |
|---|---|---|
| 1 | `s-method` | Вибір способу додавання вузла (agentless / ServiceNow / CSV) |
| 2 | `s-agentless` | Додавання одного вузла agentless (тип, адреса, параметри) |
| 3 | `s-cm-setup` | Налаштування Connection Manager (сире, "rough") |
| 4 | `cmdb-credentials` | Введення/вибір креденшелів при підключенні |
| 5 | `secret-server-config` | Конфіг Secret Server (upsell до Pro) |
| 6 | `scan-schedule` | Розклад сканування (частота, час) |
| 7 | `scan-failed` | Екран невдалого скану (найкраще зроблений) |
| 8 | `compare-scans` | Порівняння сканів / варіант Scan History |
| 9 | `csv-import` | Завантаження CSV зі списком вузлів |
| 10 | `csv-importing` | Стан "йде імпорт" (loading) |
| 11 | `csv-preview` | Прев'ю даних перед імпортом |
| 12 | `csv-complete` | Результат імпорту CSV |
| 13 | `reports` | Сторінка звітів (з CSV/PDF export) |

**Групування по темах:**
- Додавання вузлів (5): `s-method`, `s-agentless`, `s-cm-setup`, `cmdb-credentials`, `csv-import`
- CSV-флоу (4): `csv-import`, `csv-importing`, `csv-preview`, `csv-complete`
- Сканування (3): `scan-schedule`, `scan-failed`, `compare-scans`
- Креденшели (1): `secret-server-config`
- Звіти (1): `reports`

### 4. Перші питання та суперечності

**Відкриті питання (у Slack):**
- Який wireframe = джерело правди?
- Reports: старий scope казав "export НЕ в scope", липневий пакет включає — UX-концепт
  чи реальний CSV/PDF?
- Які scan-failures Guardian реально детектить?
- AWS OIDC — перевикористати патерн з Foundry?

**Проблеми узгодженості (7):**
- термінологія node/device вживається впереміш
- числа не б'ються (4 vs 5 devices, 78 vs 82% compliance)
- непослідовна back-навігація (`← Back` btn vs стрілка-іконка)
- кольори кнопок без системи (фіолетовий/синій/зелений)
- top-nav label на Scans не збігається
- Integrations = плоский список (треба Core + capabilities, Note 2 Гарета)
- структурний баг хедера (button обгортав заголовок) — виправлено на Edit/Add Check

## Оцінка екранів
- **keep:** `scan-failed`
- **rebuild:** Connection Manager
- **decision:** Scan History (його vs наш)
- **решта:** polish

## Далі (Вівторок)
Consolidated Essentials Flow (один наскрізний шлях) + рекомендація по Scan History.
